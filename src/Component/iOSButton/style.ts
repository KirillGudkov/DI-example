import {StyleSheet} from "react-native";

export const style = StyleSheet.create({
  container: {
    paddingVertical: 6,
    paddingHorizontal: 14,
    borderWidth: 1,
    borderRadius: 4,
  },
  title: {
    fontSize: 16
  }
});
