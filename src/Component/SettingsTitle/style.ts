import {StyleSheet} from "react-native";

export const style = StyleSheet.create({
  container: {
    padding: 12,
    justifyContent: 'center',
    width: '100%'
  },
  title: {
    opacity: 0.5,
    fontSize: 15,
  },
});
